import random

import math

def calculateFitness(routes, distance):
    TotalDistance = sum(routes[1] ) for i in range(len(routes))
    if TotalDistance <= distance :
        distance = TotalDistance
        return distance
    else:
        return distance
    
def 
    





